{
  "name": "galaxy_calc",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=11"
  },
  "scripts": {
    "jest": "LOG_LEVEL=silent NODE_ENV=test jest --coverage --detectOpenHandles",
    "start": "babel-node . --presets=env",
    "dev": "nodemon --inspect . --exec babel-node --presets env"
  },
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0",
    "colors": "^1.3.3"
  },
  "devDependencies": {
    "prettier": "^1.14.3",
    "nodemon": "^1.18.6",
    "jest": "^23.6.0",
    "babel-jest": "^20.0.3"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "description": "universal calculator, bc easier is better"
}
